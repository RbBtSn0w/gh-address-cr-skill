Fixed in `<commit_hash>`.

Severity: `P2`

What I changed:
- `<file_path>`: `<fix summary>`

Why this addresses the CR:
- `<behavioral correction>`
- Medium-severity path validated and aligned with expected workflow.

Validation:
- `<test_command>`
- Result: `<pass/fail + key signal>`

If you want, I can also run a broader suite before merge.
