I confirmed this CR is still valid and not fully fixed yet.

Minimal repair plan:
1. Update `<file_path>` to `<specific change>`.
2. Add or adjust test `<test_name>` for regression coverage.
3. Run `<test_command>` and report results here.

I will keep this thread open until the patch and evidence are posted.
