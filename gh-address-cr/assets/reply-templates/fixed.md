Fixed in `<commit_hash>`.

What I changed:
- `<file_path_1>`: `<brief fix summary>`
- `<file_path_2>`: `<brief fix summary>`

Why this addresses the CR:
- `<technical reasoning tied to the comment>`

Validation:
- `<test_command>`
- Result: `<pass/fail + key output>`

If anything still looks off, I can follow up with a focused patch.
